// const array = ((arr:number[])=>{
//   const numbers = arr
  
//   // numbers.sort((a, b) => (a < b) ? -1 : 1);
//   // numbers.push(60)
//   // console.log(numbers)
//   let temp:number
//   for (let i = 0; i < numbers.length-1; i++) {
//      for (let j = i+1; j < numbers.length; j++) {
//        if(numbers[i] > numbers[j]) {
//          temp = numbers[i]
//          numbers[i] = numbers[j]
//          numbers[j] = temp
//        }
//       }
//   }
//   console.log(numbers)
// })
// // const numbers:number[] = [10,20,30,40,50]
// array([20,10,50,40,30,1])